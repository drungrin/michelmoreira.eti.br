<!-- Long-form content wrapper. Theme awareness comes from the semantic
     variable overrides on .prose in global.css, not from a dark-variant
     prose class. -->
<div class="prose prose-lg">
  <slot />
</div>
